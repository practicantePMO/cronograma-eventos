# Toma la imagen oficial de SearXNG y le mete nuestra configuracion.
# Render construye esta imagen automaticamente al conectar el repositorio.
FROM searxng/searxng:latest

COPY settings.yml /etc/searxng/settings.yml
